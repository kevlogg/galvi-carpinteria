rules_version = '2';
// Lectura pública para imágenes de productos. Escritura con autenticación.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
